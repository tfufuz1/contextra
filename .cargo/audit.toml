# Cargo Audit configuration for Contextra workspace
# Documenting isolation proofs for transitive security advisories in non-core crates.

[advisories]
ignore = [
    # RUSTSEC-2026-0176: pyo3 0.24.2 Out-of-bounds read in PyList/PyTuple iterators
    # ISOLATION PROOF: `pyo3` is used exclusively in `contextra-py` for FFI Python bindings.
    # `contextra-py` does not invoke `PyList` or `PyTuple` `.nth()` / `.nth_back()` iterator methods.
    # Isolated from `contextra-crypto` and core engine kernel.
    # Review Date: 2026-12-31
    "RUSTSEC-2026-0176",

    # RUSTSEC-2026-0177: pyo3 0.24.2 Missing Sync bound on PyCFunction::new_closure
    # ISOLATION PROOF: `pyo3` is used exclusively in `contextra-py` for FFI Python bindings.
    # `contextra-py` does not use `PyCFunction::new_closure`.
    # Isolated from `contextra-crypto` and core engine kernel.
    # Review Date: 2026-12-31
    "RUSTSEC-2026-0177",
]
