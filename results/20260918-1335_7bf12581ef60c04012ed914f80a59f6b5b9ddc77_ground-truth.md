# Ground-Truth Audit Report (Diagnose-Snapshot, CI-Historie, Branch-Protection, hnsw.rs-Re-Check)

- **Timestamp (UTC):** 2026-09-18T13:35:00Z
- **Commit-Hash:** `7bf12581ef60c04012ed914f80a59f6b5b9ddc77`
- **Session:** Audit Session (Google-Jules)
- **Role Mode:** AUDIT (no production code modifications)

---

## 1. REFERENCE WORKSPACE RUNS (RECP-IST-01)

### 1.1 `cargo check --workspace`
- **Result:** PASSED (with minor warnings)
- **Status:** Standard compilation succeeds across all workspace crates.
- **Warnings:**
  - `memfuse-core-ipc-gen`: `flatc` not found, using existing generated code.
  - `xtask`: 5 unused function warnings in `check_unwrap_ratchet.rs` (`fnv1a_hash`, `scan_unwrap_expect_occurrences`, `collect_rs_files`, `run_check_unwrap_ratchet`, `save_baseline`) because `check_unwrap_ratchet.rs` is a standalone module file whose function entry points are called via subcommand dispatch or tests.
  - `xtask`: 2 `unused Result` warnings in `check_bandit_latency_budget.rs` (`profile_state.update`).
  - `memfuse-agent`: 1 deprecation warning (`engine::OrchestratorEngine::new` -> `try_new`).

### 1.2 `cargo clippy --workspace -- -D warnings`
- **Result:** FAILED (2 errors in `crates/memfuse-index/src/hnsw.rs`)
- **Status:** Clippy check fails due to `-D warnings` enforcement on `clippy::too_many_arguments`.
- **Exact Errors:**
  1. `crates/memfuse-index/src/hnsw.rs:1309:1`:
     ```
     error: this function has too many arguments (8/7)
      --> crates/memfuse-index/src/hnsw.rs:1309:1
       |
     1309 | fn get_neighbor_conns_in_batch(
       | ^
     ```
  2. `crates/memfuse-index/src/hnsw.rs:2085:5`:
     ```
     error: this function has too many arguments (8/7)
      --> crates/memfuse-index/src/hnsw.rs:2085:5
       |
     2085 |     fn search_layer_with_context(
       |     ^
     ```
- **Note on `clippy::useless_conversion`:** No `clippy::useless_conversion` warnings/errors were triggered.

### 1.3 `cargo test --workspace`
- **Result:** FAILED (2 test failures in `crates/memfuse-bench`)
- **Status:** Most crate unit and integration test suites pass cleanly across the workspace, but `memfuse-bench` fails on 2 PathRAG sweep integration tests.
- **Failed Tests:**
  1. `test_pathrag_sweep_locomo_execution` in `crates/memfuse-bench/tests/external_benchmarks_test.rs`:
     `Error: SnapshotUnsupportedForSignal("PathRag strategy does not support snapshot-isolated retrieval")`
  2. `test_pathrag_sweep_long_mem_eval_execution` in `crates/memfuse-bench/tests/external_benchmarks_test.rs`:
     `Error: SnapshotUnsupportedForSignal("PathRag strategy does not support snapshot-isolated retrieval")`

---

## 2. CI HISTORY & BRANCH PROTECTION STATUS

- **`gh` CLI Status:**
  - Command `gh run list` and `gh api` failed with `-bash: gh: command not found`.
  - **Verdict:** `gh` binary is NOT installed or authenticated in the current execution environment.
  - **Documentation:** Remote GitHub Actions run history (`merge-gate.yml`, `post-merge-verification.yml`) and GitHub Branch Protection API rules cannot be directly queried via `gh` CLI from this environment.

---

## 3. ANHANG- & QUELLCODE-VERGLEICH PROTOKOLL (`hnsw.rs`)

### 3.1 Commit History Verification
- Command: `git log --oneline --follow -- crates/memfuse-index/src/hnsw.rs`
- **Result:** Output returned only commit `7bf1258` on the single-commit shallow/isolated main branch history in this VM instance.
- **Commit Lookup:** `git show 5decebb 0a9c952` returned `fatal: ambiguous argument '5decebb': unknown revision or path not in the working tree.` because the local checkout is a shallow clone (`git rev-list --count HEAD` = 1).

### 3.2 Current Content Inspection around Lines 857 and 1992
- **Line 857:**
  ```rust
  let regions = crate::partial_rebuild::should_trigger_partial_rebuild(
      &tracker,
      &std_map,
      global_tombstone_ratio,
      &self.inner.cold.config.partial_rebuild_config,
  );
  ```
- **Line 1992:**
  ```rust
  let latest_nodes = self.hot.nodes.read();
  if let Some(node) = latest_nodes.get(ram_idx) {
      return self.compute_distance_with_data(query, query_q, &node.vector, q_ref);
  }
  ```

### 3.3 Findings & Binärer Befund (`hnsw.rs`)
- **Status:** **Bereits behoben (bezüglich `clippy::useless_conversion`).**
- **Evidence:**
  1. Neither line 857 nor line 1992 contains any conversion patterns (`u64::from(...)`, `.into()`, `as u64`). Line 857 handles partial rebuild trigger checking, and line 1992 handles vector resolution for candidate nodes.
  2. Isolated execution of `cargo clippy -p memfuse-index -- -D warnings` did NOT trigger any `clippy::useless_conversion` warning.
  3. **New finding in `hnsw.rs`:** `cargo clippy -p memfuse-index` currently fails on `clippy::too_many_arguments` for `get_neighbor_conns_in_batch` (8 args) and `search_layer_with_context` (8 args).
- **Recommendation for next wave:** Refactor `get_neighbor_conns_in_batch` and `search_layer_with_context` argument lists into context structs (e.g., `BatchNeighborContext` / `LayerSearchContext`) or suppress/allow where appropriate in a dedicated FIX wave.

---

## 4. UNWRAP RATCHET ARCHITECTURE ANALYSIS

- **Question:** Does `check-unwrap-baseline` / `check-unwrap-ratchet` test against `docs/unwrap_baseline_history.jsonl` or against a live-calculated value from `.unwrap-baseline.json`?
- **Analysis of `xtask/src/check_unwrap_ratchet.rs`:**
  1. Function `run_check_unwrap_ratchet(root, update_mode)` reads **`.unwrap-baseline.json`** at the repository root.
  2. `scan_unwrap_expect_occurrences(root)` dynamically scans all production `.rs` files under `crates/` (excluding `/tests/`, `/benches/`, `/examples/`, `_test.rs`), calculates FNV-1a hashes of 3-line windows around each `.unwrap()` / `.expect()`, and generates a live `current_entries` list.
  3. In default check mode (`update_mode = false`), `run_check_unwrap_ratchet` compares `current_entries` against entries stored in **`.unwrap-baseline.json`**.
  4. If new unapproved unwraps/expect calls are found in production code that are not present in `.unwrap-baseline.json`, the ratchet check FAILS (`❌ [IP-12 RATCHET VIOLATION]`).
  5. **`docs/unwrap_baseline_history.jsonl`** is a historical audit log / record file (like mutation score history) and is **NOT** used by `cargo xtask check-unwrap-ratchet` to validate live PR changes.
- **Conclusion:** `check-unwrap-ratchet` calculates live occurrences from production code and compares them strictly against `.unwrap-baseline.json`.

---

## 5. SUMMARY OF ACTIONABLE FINDINGS FOR FUTURE WAVES

1. **`clippy::too_many_arguments` in `hnsw.rs`:**
   - Functions `get_neighbor_conns_in_batch` (line 1309) and `search_layer_with_context` (line 2085) take 8 arguments, exceeding Clippy's default limit of 7.
2. **PathRAG Snapshot Error in `memfuse-bench`:**
   - Tests `test_pathrag_sweep_locomo_execution` and `test_pathrag_sweep_long_mem_eval_execution` fail with `SnapshotUnsupportedForSignal`.
3. **Environment Dependency:**
   - `gh` CLI is not installed in the execution environment; workflow status checks relying on GitHub API cannot run locally in this environment without installing `gh`.
